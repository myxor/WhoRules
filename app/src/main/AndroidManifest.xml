<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.marcoheiming.whorules">

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme"
        android:testOnly="false"
        android:fullBackupContent="true"
        android:allowBackup="true">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/title_activity_about" />
        <activity
            android:name=".VasallsListActivity"
            android:label="@string/title_vasalls_list"/>
        <activity
            android:name=".TimerActivity"
            android:label="@string/title_timer" />
        <activity
            android:name=".RanksListActivity"
            android:label="@string/title_ranks"/>

        <activity
            android:name=".BeersListActivity"
            android:label="@string/beers"/>

    </application>

</manifest>